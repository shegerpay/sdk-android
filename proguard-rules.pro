# ShegerPay Android SDK library ProGuard rules.
# Minification is disabled for release builds (isMinifyEnabled = false), so no
# rules are required here. Consumer-facing keep rules live in consumer-rules.pro.
